#technologies
  h2 Technologies
  .container-fluid.d-flex
    .container.mx-auto.justify-content-center
      .row
        each technology in technologies
          .card.col-6.col-lg-3
            img.img-fluid.rounded(src=`/uploads/homepage-technologies/${technology.image}`)
            h5= technology.name
            p= technology.description